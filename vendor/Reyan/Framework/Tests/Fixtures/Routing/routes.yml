show_post:
    pattern: /post/:slug/:id
    controller: BlogBundle:Post:Show
    requirements:
        id: \d+

create_post:
    pattern: /admin/post/create
    controller: BackendBlogBundle:Post:Create
